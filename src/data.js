// estas funciones son de ejemplo

/*export const tarjetas = (data) => {
  let tarjetaString = "";
  for (const contenedor of data.pokemon) {
    tarjetaString += ` 
     <div class="back tarjeta" >
        <p> Ataque:<br> ${contenedor.stats["base-attack"]}
        <br>Defensa:<br> ${contenedor.stats["base-defense"]} 
        <br>Resisencia:<br> ${contenedor.stats["base-stamina"]} 
        <br>Max-cp:<br> ${contenedor.stats["max-cp"]}
        <br>Max-hp:<br> ${contenedor.stats["max-hp"]}</p>
      </div>
    </div>`;
   
  }
  return tarjetaString;
}*/
    

